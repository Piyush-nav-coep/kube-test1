apiVersion: v1
kind: Service
metadata:
  name: hello-python
spec:
  selector:
    app: hello-python
  ports:
  - protocol: "TCP"
    port: 5001
    targetPort: 5000
  type: LoadBalancer

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-python
spec:
  selector:
    matchLabels:
      app: hello-python
  replicas: 4
  template:
    metadata:
      labels:
        app: hello-python
    spec:
      containers:
      - name: hello-python
        image: piyushnav/first-build-on-dockerhub:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5000

